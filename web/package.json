{
  "name": "finance-track-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && npm run keep",
    "keep": "node -e \"require('fs').writeFileSync('dist/.gitkeep','')\"",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^1.3.0"
  },
  "dependencies": {
    "preact": "^10.29.8"
  }
}
